# Entorno WSL

SIEMPRE usar `git.exe` para TODAS las operaciones git (enforcement por hook).
Operaciones permitidas: checkout, branch, commit, log, diff, status, stash, pull, push, fetch.
Si `git.exe` falla con "Exec format error": WSL Interop cayo. Pedir al usuario que ejecute desde PowerShell.
